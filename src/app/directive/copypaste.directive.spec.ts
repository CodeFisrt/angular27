import { CopypasteDirective } from './copypaste.directive';

describe('CopypasteDirective', () => {
  it('should create an instance', () => {
    const directive = new CopypasteDirective();
    expect(directive).toBeTruthy();
  });
});
